[package]
name = "cni-host-local"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "host-local"
path = "src/main.rs"

[dependencies]
cni = { path = "../cni" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ipnetwork = "0.20"
fs2 = "0.4"

[dev-dependencies]
tempfile = "3"
