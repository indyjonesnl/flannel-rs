[package]
name = "cni-portmap"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "portmap"
path = "src/main.rs"

[dependencies]
cni = { path = "../cni" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ipnetwork = "0.20"
