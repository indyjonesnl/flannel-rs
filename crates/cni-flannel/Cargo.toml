[package]
name = "cni-flannel"
version.workspace = true
edition = "2021"

[[bin]]
name = "flannel"
path = "src/main.rs"

[dependencies]
cni = { path = "../cni" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
